name: Security

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  schedule:
    - cron: "23 11 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  security:
    permissions:
      contents: read
      actions: read
      security-events: write
    uses: forkwright/.github/.github/workflows/security.yml@5556cb9e05327f855ccc208080fb1924cf7e8264
    with:
      has_private_deps: false
      run_osv: true
      cargo_audit_timeout_minutes: 15
